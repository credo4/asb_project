# ASB — Espace speaker

Interface Vue 3 permettant à chaque intervenant de gérer son profil (bio,
médias, disponibilités, missions) après soumission à la revue admin.

Consomme l'API exposée par [`../backend`](../backend). Rôle concerné :
`SPEAKER`.

**Statut** : pas encore initialisé. Le scaffold Vue 3 + Vite sera fait en
Phase 2 (voir `CLAUDE.md` à la racine du monorepo).
