// Délai de réponse par défaut si l'admin n'en précise pas un explicitement
// à l'envoi (§3.2) — configurable via AVAILABILITY_RESPONSE_TTL_DAYS, même
// pattern que DEFAULT_INVITATION_TOKEN_TTL_DAYS (roster-application.constants.ts).
export const DEFAULT_AVAILABILITY_RESPONSE_DAYS = 7;
